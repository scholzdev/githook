foreach git.staged_files { f in
    let ext = f.extension
    let has_rs = ext.ends_with(".rs")

    if has_rs {
      run "echo File: ${f.name}, Path: ${f.path.string}, Basename: ${f.path.basename}"
      run "echo '  → Contains test, Parent: ${f.path.parent}'"
    }
    break
}
